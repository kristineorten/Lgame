<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="no.lgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GameOverOnePlayerActivity"></activity>
        <activity android:name=".RulesActivity" />
        <activity android:name=".GameOverActivity" />
        <activity android:name=".StatsActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".RulesActivity2" />
        <activity android:name=".OnePlayerActivity" />
        <activity android:name=".LevelActivity" />
        <activity android:name=".TwoPlayerActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>